---
import Layout from "../layouts/Layout.astro";
import "../styles/onlineShop.css";
---

<Layout title="PlayGames - Shop | Spartanburg's Premier Card & Game Shop">
    <div class="shop-page">
        <div class="container">
            <!-- Header -->
            <header class="page-header">
                <h1 class="page-title">Shop Our Collection</h1>
                <p class="page-subtitle">
                    Browse our selection of premium trading cards, board games, and collectibles
                </p>
            </header>

            <!-- TCGPlayer Store Banner -->
            <div class="tcgplayer-banner">
                <div class="banner-content">
                    <svg class="banner-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"></path>
                    </svg>
                    <div class="banner-text">
                        <h2>Visit Our Full Online Store</h2>
                        <p>Browse thousands of cards and complete your collection on TCGPlayer</p>
                    </div>
                    <a href="https://store.tcgplayer.com/" target="_blank" rel="noopener noreferrer" class="banner-button">
                        Visit TCGPlayer Store
                        <svg class="button-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
                        </svg>
                    </a>
                </div>
            </div>

            <!-- Search and Filter Section -->
            <div class="search-filter-section">
                <div class="search-bar">
                    <svg class="search-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                    </svg>
                    <input
                            type="text"
                            id="searchInput"
                            placeholder="Search products..."
                            class="search-input"
                    />
                </div>

                <div class="filter-section">
                    <label for="categoryFilter" class="filter-label">Category:</label>
                    <select id="categoryFilter" class="filter-select">
                        <option value="all">All Products</option>
                        <option value="trading-cards">Trading Cards</option>
                        <option value="board-games">Board Games</option>
                        <option value="comics">Comic Books</option>
                        <option value="figurines">Figurines</option>
                    </select>

                    <label for="sortFilter" class="filter-label">Sort By:</label>
                    <select id="sortFilter" class="filter-select">
                        <option value="default">Default</option>
                        <option value="price-low">Price: Low to High</option>
                        <option value="price-high">Price: High to Low</option>
                        <option value="name">Name: A-Z</option>
                    </select>
                </div>
            </div>

            <!-- Results Info -->
            <div class="results-info">
                <p id="resultsCount">Showing <span id="currentCount">0</span> of <span id="totalCount">0</span> products</p>
            </div>

            <!-- Products Grid -->
            <div id="productsGrid" class="products-grid">
                <!-- Products will be dynamically inserted here -->
            </div>

            <!-- No Results Message -->
            <div id="noResults" class="no-results hidden">
                <svg class="no-results-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
                <h3>No products found</h3>
                <p>Try adjusting your search or filters</p>
            </div>

            <!-- Pagination -->
            <div id="pagination" class="pagination">
                <button id="prevPage" class="pagination-button" disabled>
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                    </svg>
                    Previous
                </button>

                <div id="pageNumbers" class="page-numbers">
                    <!-- Page numbers will be inserted here -->
                </div>

                <button id="nextPage" class="pagination-button">
                    Next
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                    </svg>
                </button>
            </div>
        </div>
    </div>
</Layout>

<script>
    // Product data
    const products = [
        {
            id: 1,
            name: "Black Lotus - Alpha Edition",
            category: "trading-cards",
            price: 25000.00,
            description: "Extremely rare Alpha Black Lotus in near-mint condition. A true collector's gem.",
            image: "placeholder",
            inStock: true
        },
        {
            id: 2,
            name: "Charizard VMAX - Champions Path",
            category: "trading-cards",
            price: 450.00,
            description: "Rainbow rare Charizard VMAX from Champions Path. Graded PSA 10.",
            image: "placeholder",
            inStock: true
        },
        {
            id: 3,
            name: "Blue-Eyes White Dragon - LOB 1st Edition",
            category: "trading-cards",
            price: 3500.00,
            description: "1st Edition Legend of Blue Eyes White Dragon. Near mint condition.",
            image: "placeholder",
            inStock: false
        },
        {
            id: 4,
            name: "Gloomhaven Board Game",
            category: "board-games",
            price: 140.00,
            description: "Epic campaign game with legacy elements. Perfect for strategy enthusiasts.",
            image: "placeholder",
            inStock: true
        },
        {
            id: 5,
            name: "Wingspan Deluxe Edition",
            category: "board-games",
            price: 95.00,
            description: "Award-winning bird collection game with beautiful artwork and components.",
            image: "placeholder",
            inStock: true
        },
        {
            id: 6,
            name: "The Amazing Spider-Man #1 (Reprint)",
            category: "comics",
            price: 45.00,
            description: "High-quality reprint of the iconic first appearance of Spider-Man.",
            image: "placeholder",
            inStock: true
        },
        {
            id: 7,
            name: "Batman: The Killing Joke",
            category: "comics",
            price: 24.99,
            description: "Deluxe edition of Alan Moore's acclaimed graphic novel.",
            image: "placeholder",
            inStock: true
        },
        {
            id: 8,
            name: "Funko Pop! Darth Vader (Chrome)",
            category: "figurines",
            price: 75.00,
            description: "Limited edition chrome Darth Vader Funko Pop. Collector's item.",
            image: "placeholder",
            inStock: true
        },
        {
            id: 9,
            name: "Nendoroid Link - Breath of the Wild",
            category: "figurines",
            price: 65.00,
            description: "Highly detailed poseable Link figure with multiple accessories.",
            image: "placeholder",
            inStock: true
        },
        {
            id: 10,
            name: "Force of Will - Alliances",
            category: "trading-cards",
            price: 180.00,
            description: "Near mint Force of Will from Alliances. Iconic counterspell.",
            image: "placeholder",
            inStock: true
        },
        {
            id: 11,
            name: "Settlers of Catan",
            category: "board-games",
            price: 49.99,
            description: "Classic resource management and trading game for 3-4 players.",
            image: "placeholder",
            inStock: true
        },
        {
            id: 12,
            name: "Ticket to Ride - Europe",
            category: "board-games",
            price: 54.99,
            description: "Cross-country train adventure game. Family-friendly and strategic.",
            image: "placeholder",
            inStock: true
        },
        {
            id: 13,
            name: "X-Men #1 - Jim Lee Edition",
            category: "comics",
            price: 120.00,
            description: "First issue of the iconic 1991 X-Men series. Near mint condition.",
            image: "placeholder",
            inStock: false
        },
        {
            id: 14,
            name: "Pikachu - 1st Edition Base Set",
            category: "trading-cards",
            price: 850.00,
            description: "Red cheeks shadowless 1st edition Pikachu. PSA 9 graded.",
            image: "placeholder",
            inStock: true
        },
        {
            id: 15,
            name: "Figma Samus Aran",
            category: "figurines",
            price: 89.99,
            description: "Highly articulated Samus Aran action figure from Metroid series.",
            image: "placeholder",
            inStock: true
        },
        {
            id: 16,
            name: "Pandemic Legacy: Season 1",
            category: "board-games",
            price: 69.99,
            description: "Cooperative legacy game where your choices permanently alter the game.",
            image: "placeholder",
            inStock: true
        },
        {
            id: 17,
            name: "Mox Ruby - Unlimited Edition",
            category: "trading-cards",
            price: 4200.00,
            description: "Power Nine card in excellent condition. A must-have for collectors.",
            image: "placeholder",
            inStock: false
        },
        {
            id: 18,
            name: "Walking Dead #1 (Replica)",
            category: "comics",
            price: 19.99,
            description: "High-quality replica of the first issue of The Walking Dead.",
            image: "placeholder",
            inStock: true
        }
    ];

    // Pagination settings
    const ITEMS_PER_PAGE = 9;
    let currentPage = 1;
    let filteredProducts = [...products];

    // DOM elements
    const productsGrid = document.getElementById('productsGrid');
    const searchInput = document.getElementById('searchInput');
    const categoryFilter = document.getElementById('categoryFilter');
    const sortFilter = document.getElementById('sortFilter');
    const prevPageBtn = document.getElementById('prevPage');
    const nextPageBtn = document.getElementById('nextPage');
    const pageNumbers = document.getElementById('pageNumbers');
    const noResults = document.getElementById('noResults');
    const currentCount = document.getElementById('currentCount');
    const totalCount = document.getElementById('totalCount');

    // Format price
    function formatPrice(price) {
        return new Intl.NumberFormat('en-US', {
            style: 'currency',
            currency: 'USD'
        }).format(price);
    }

    // Create product card
    function createProductCard(product) {
        const card = document.createElement('div');
        card.className = 'product-card';

        card.innerHTML = `
            <div class="product-image-container">
                <div class="product-image">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                    </svg>
                </div>
                ${!product.inStock ? '<div class="out-of-stock-badge">Out of Stock</div>' : ''}
            </div>
            <div class="product-info">
                <h3 class="product-name">${product.name}</h3>
                <p class="product-category">${getCategoryName(product.category)}</p>
                <p class="product-description">${product.description}</p>
            <div class="product-footer">
                <span class="product-price">${formatPrice(product.price)}</span>
                ${product.inStock
            ? `<a href="https://store.tcgplayer.com" target="_blank" rel="noopener noreferrer" class="view-button">View</a>`
            : `<span class="view-button disabled">Out of Stock</span>`
        }
            </div>
            </div>
        `;

        return card;
    }

    // Get category display name
    function getCategoryName(category) {
        const names = {
            'trading-cards': 'Trading Cards',
            'board-games': 'Board Games',
            'comics': 'Comic Books',
            'figurines': 'Figurines'
        };
        return names[category] || category;
    }

    // Filter products
    function filterProducts() {
        const searchTerm = searchInput.value.toLowerCase();
        const category = categoryFilter.value;

        filteredProducts = products.filter(product => {
            const matchesSearch = product.name.toLowerCase().includes(searchTerm) ||
                product.description.toLowerCase().includes(searchTerm);
            const matchesCategory = category === 'all' || product.category === category;
            return matchesSearch && matchesCategory;
        });

        sortProducts();
        currentPage = 1;
        renderProducts();
    }

    // Sort products
    function sortProducts() {
        const sortBy = sortFilter.value;

        switch(sortBy) {
            case 'price-low':
                filteredProducts.sort((a, b) => a.price - b.price);
                break;
            case 'price-high':
                filteredProducts.sort((a, b) => b.price - a.price);
                break;
            case 'name':
                filteredProducts.sort((a, b) => a.name.localeCompare(b.name));
                break;
            default:
                // Keep original order
                break;
        }
    }

    // Render products
    function renderProducts() {
        productsGrid.innerHTML = '';

        const startIndex = (currentPage - 1) * ITEMS_PER_PAGE;
        const endIndex = startIndex + ITEMS_PER_PAGE;
        const currentProducts = filteredProducts.slice(startIndex, endIndex);

        if (filteredProducts.length === 0) {
            noResults.classList.remove('hidden');
            productsGrid.classList.add('hidden');
            document.getElementById('pagination').classList.add('hidden');
        } else {
            noResults.classList.add('hidden');
            productsGrid.classList.remove('hidden');
            document.getElementById('pagination').classList.remove('hidden');

            currentProducts.forEach(product => {
                productsGrid.appendChild(createProductCard(product));
            });
        }

        updatePagination();
        updateResultsCount();
    }

    // Update results count
    function updateResultsCount() {
        const startIndex = (currentPage - 1) * ITEMS_PER_PAGE + 1;
        const endIndex = Math.min(currentPage * ITEMS_PER_PAGE, filteredProducts.length);

        currentCount.textContent = filteredProducts.length === 0 ? '0' : `${startIndex}-${endIndex}`;
        totalCount.textContent = filteredProducts.length;
    }

    // Update pagination
    function updatePagination() {
        const totalPages = Math.ceil(filteredProducts.length / ITEMS_PER_PAGE);

        // Update prev/next buttons
        prevPageBtn.disabled = currentPage === 1;
        nextPageBtn.disabled = currentPage === totalPages || totalPages === 0;

        // Generate page numbers
        pageNumbers.innerHTML = '';

        for (let i = 1; i <= totalPages; i++) {
            // Show first page, last page, current page, and pages around current
            if (i === 1 || i === totalPages || (i >= currentPage - 1 && i <= currentPage + 1)) {
                const pageBtn = document.createElement('button');
                pageBtn.className = `page-number ${i === currentPage ? 'active' : ''}`;
                pageBtn.textContent = i;
                pageBtn.addEventListener('click', () => {
                    currentPage = i;
                    renderProducts();
                    window.scrollTo({ top: 0, behavior: 'smooth' });
                });
                pageNumbers.appendChild(pageBtn);
            } else if (i === currentPage - 2 || i === currentPage + 2) {
                const ellipsis = document.createElement('span');
                ellipsis.className = 'page-ellipsis';
                ellipsis.textContent = '...';
                pageNumbers.appendChild(ellipsis);
            }
        }
    }

    // Event listeners
    searchInput.addEventListener('input', filterProducts);
    categoryFilter.addEventListener('change', filterProducts);
    sortFilter.addEventListener('change', () => {
        sortProducts();
        renderProducts();
    });

    prevPageBtn.addEventListener('click', () => {
        if (currentPage > 1) {
            currentPage--;
            renderProducts();
            window.scrollTo({ top: 0, behavior: 'smooth' });
        }
    });

    nextPageBtn.addEventListener('click', () => {
        const totalPages = Math.ceil(filteredProducts.length / ITEMS_PER_PAGE);
        if (currentPage < totalPages) {
            currentPage++;
            renderProducts();
            window.scrollTo({ top: 0, behavior: 'smooth' });
        }
    });

    // Initial render
    renderProducts();
</script>