<nav>
    <div class="nav-container">
        <div class="logo"><a href="/">⚔️ PlayGames</a></div>
        <ul class="nav-links">
            <li><a href="/">Home</a></li>
            <li><a href="/events">Events</a></li>
            <li><a href="/onlineShop">Card Shop</a></li>
            <li><a href="/aboutUs">About Us</a></li>            
            <li><a href="/contact">Contact</a></li>
        </ul>
    </div>
</nav>

<style>
    /* Navbar */
    nav {
        background: rgba(15, 12, 41, 0.95);
        backdrop-filter: blur(10px);
        padding: 1rem;
        position: sticky;
        top: 0;
        z-index: 1000;
        border-bottom: 2px solid #6c63ff;
        box-shadow: 0 4px 20px rgba(108, 99, 255, 0.3);
    }

    .nav-container {
        max-width: 1400px;
        margin: 0 auto;
        display: flex;
        flex-direction: column;
        gap: 1rem;
        align-items: center;
    }

    .logo {
        font-size: 1.5rem;
        font-weight: bold;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        letter-spacing: 1px;
    }
    
    .logo a {
        text-decoration: none;        
    }

    .nav-links {
        display: flex;
        flex-wrap: wrap;
        gap: 1rem;
        list-style: none;
        justify-content: center;
    }

    .nav-links a {
        color: #e0e0e0;
        text-decoration: none;
        font-weight: 500;
        transition: all 0.3s ease;
        position: relative;
        font-size: 0.95rem;
        padding: 0.5rem;
    }

    .nav-links a::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 0;
        width: 0;
        height: 2px;
        background: linear-gradient(90deg, #667eea, #764ba2);
        transition: width 0.3s ease;
    }

    .nav-links a:hover {
        color: #6c63ff;
    }

    .nav-links a:hover::after {
        width: 100%;
    }

    /* Tablet and up */
    @media (min-width: 768px) {
        nav {
            padding: 1.2rem 2rem;
        }

        .nav-container {
            flex-direction: row;
            justify-content: space-between;
        }

        .logo {
            font-size: 1.8rem;
        }

        .nav-links {
            gap: 2rem;
        }

        .nav-links a {
            font-size: 1rem;
            padding: 0;
        }

        .nav-links a::after {
            bottom: -5px;
        }
    }
</style>